sudo: required

services:
  - docker

before_install:
  - docker build -t raniemi/elixir_examples
  - ./bin/docker-run "mix local.rebar --force"

script:
  - ./mix/test-cover

after_script:
  - ./bin/docker-run "mix deps.get --only docs; MIX_ENV=docs mix inch.report"

notifications:
  recipients:
    - dev@lone-cyprus.com
